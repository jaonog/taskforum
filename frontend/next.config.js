// next.config.js (VERSÃO NOVA E CORRETA) ✅

/** @type {import('next').NextConfig} */
const nextConfig = {
  // suas configurações aqui, por exemplo:
  reactStrictMode: true,
}

module.exports = nextConfig